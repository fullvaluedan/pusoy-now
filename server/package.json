{
  "name": "pusoy-now-server",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: better-auth (Hono + D1) for Pusoy Now. Kept out of the Expo/Metro bundle by living in its own package.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/index.test.ts"
  },
  "dependencies": {
    "better-auth": "1.6.23",
    "hono": "4.12.29",
    "kysely": "^0.28.17 || ^0.29.0",
    "kysely-d1": "0.4.0",
    "stripe": "22.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250000.0 || ^5.0.0",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.0",
    "typescript": "~6.0.3",
    "wrangler": "^4.106.0"
  }
}
